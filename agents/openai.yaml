interface:
  display_name: "Ship the Work"
  short_description: "Turn one idea into a complete, usable deliverable within 30 days."
  default_prompt: "Use Ship the Work to identify the smallest complete deliverable, cut nonessential scope, and produce verified tangible progress in this session."
policy:
  allow_implicit_invocation: true
